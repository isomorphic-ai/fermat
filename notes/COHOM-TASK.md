# TASK COHOMOLOGY-LEDGER — credit is cohomology; the frontier is faithfulness

**TruthSeed:** `fermat-cohom:every-cochain-is-inherited-flow-unresolved-potential-or-outgoing-action`
**For:** PRO Goblin (GPT 5.6 Sol Ultra) via codex, working in ~/fermat.
**Provenance:** Fabian (sevenfold + the focus/attention correction:
structure supplies routes, FOCUS weights them, agency executes) + GPT
5.6 PRO (item 7 = cohomology; the ledger IS the dimension formula) +
Sol Ultra (the canonical filtration as the primary object; the
frontier as one injectivity theorem). Standing rules: NO unconditional
7a, NO endpoint, NO transformer. The cube session's verdict (outcome
4: wild route necessary) is doctrine.

## W1 — the executable cell

Fermat/Experiments/Conservation/CohomologyLedger.lean: the finite-dimensional
cochain cell - dPrev : Vprev ->l[K] V, dNext : V ->l[K] Vnext,
dNext.comp dPrev = 0; B := range dPrev, Z := ker dNext, H := Z/B,
Bout := range dNext. The cell ledger theorem:
  finrank B + finrank H + finrank Bout = finrank V
and the INSTANCE into Fermat.Conservation.Ledger (stock = finrank B,
credit = finrank H, converted = finrank Bout, total = finrank V) -
cohomology seated as the credit column, carried through
IsoConserveBridge (the tunnel's third major instance).

## W2 — the generic filtration layer (the primary object)

CohomologyExactCell: the canonical filtration 0 <= B <= Z <= C with
its TWO exact sequences (0 -> B -> Z -> H -> 0 and
0 -> Z -> C -> Bout -> 0), NO canonical splitting - extend the
mechanical no-splitting guard to this cell (the dimension equation is
the lossy shadow; the filtration retains the gluing). The generic
ledger theorem: ANY additive invariant on short exact sequences
induces the ledger identity - finrank as first observer; finite
length and v_p of cardinality as further observers where cheap. State
the hierarchy in the docstring: exact filtration -> K0 ledger ->
numeric ledger, each a progressively lossier observation.

## W3 — the automatic theorems (priority order, budget-aware)

1. Exact forms vanish on cycles (no private gain for periods).
2. Euler-Poincare: alternating sums - chi(C) = chi(H); internal
   transfers cancel pairwise; the global conservation law over the
   dimension ladder.
3. Homotopy invariance: f - g = dh + hd implies equal maps on H (the
   AreaTransfer prediction: route differences filled by area do not
   alter the potential class).
4. Short-exact-sequence Euler additivity (shared accounting across a
   split).
A finished theorem beats two halves; stop cleanly.

## W4 — the Fermat frontier restated as ONE injectivity interface

On the existing TatePairing/stage machinery: define the detector map
Lambda : H_FLT -> dual, Lambda x y = pair_59 x y (H_FLT = the
remaining difference-mode potential holding the 7a candidate).
Re-read the existing conditional master theorem as: tame vanishing +
reciprocity give Lambda x = 0 - the one-wild-column theorem IS Stokes
(each local term a boundary reading; tame faces cancel; the wild face
carries zero net flux). Then state the NEW single frontier interface:
  wild_detector_faithful : ker Lambda = 0
(the wild detector family separates the remaining potential;
Poitou-Tate nondegeneracy noted as its natural source; if H_FLT is
one-dimensional one transverse detector suffices - state the
dimension reduction). Update BOUNDARY-MAP: the frontier is no longer
"find another conservation law" - it is "prove the Stokes detector is
nondegenerate on the remaining potential." Record Fabian's axis-3
correction where it touches docs (focus = analytic weights; Transfer
spans structure/focus/alignment/agency) - docstring updates only, no
refactor.

## House rules (binding)

PREDICTIONS first (which W3 theorems land; whether the Ledger
instance is definitional; where the filtration fights Mathlib
quotients). FINDINGS at discovery. All guards including no-splitting
extended; full verification stays green; standard trio; generic;
ps first. Truthful commits.

— Fable (reviewer), on behalf of Fabian, 2026-08-07
