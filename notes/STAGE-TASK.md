# TASK COMMON-ACTION-STAGE — seat the arithmetic on the stage; derive, don't narrate

**TruthSeed:** `fermat-stage:the-narrative-walks-behind-the-algebra`
**For:** PRO Goblin (GPT 5.6 Sol Ultra) via codex, working in ~/fermat.
**Provenance:** the analysis goblin's specification (via Fabian): same
paired carrier, same obstruction, same action algebra, both receipts,
unit-ideal-or-named-factor outcome. "Only after that is a 59-specific
computation mathematics rather than numerology." The linking stage
(W1–W5, commits 72a363b..b46febc) is complete and audited. 7a remains
the GAUGE, never the target; NO transformer, NO endpoint.

## W0 — discharge erratum E1 by derivation (notes/ERRATA.md)

The 7a/7d mode labels in BOUNDARY-MAP's interaction rereading are
reversed (7a: x - y = 0 kills the DIFFERENCE; 7d: x + y = 0 kills the
COMMON mode). Now that SwapQuotient exists, FIX BY COMPUTATION: map
the two relation words into A_swap and derive their eigenspace
assignment under pi_common = (1+R)/2, pi_diff = (1-R)/2 — which word
projects where is a theorem, not a story. Update BOUNDARY-MAP W4
accordingly, cite E1, and record what the corrected assignment does to
the third-party requirement (note: the PROVEN 7d came via the relative
norm — a detour through the real subfield, itself a third structure;
say precisely what the algebra makes of that).

## W1 — the paired carrier

Build M = Sel_chi + (Sel_chi*)-dual from the campaign's own objects
via the exact sequence 0 -> units/p-th-powers -> Sel -> Cl[p] -> 0:
the Kummer/unit part from the real-units machinery, the class part
from ClassCarrier (with its receipts — the beta direction IS the
units leg of the sequence). THE DUAL MATTERS (the analysis goblin's
caution): reflection gives routes between the two components, not a
swap-equivalence; only closed words are certainly endomorphisms.
Filtered carrier, not dg (standing rule).

## W2 — the action

Instantiate the linking algebra on M: Lambda acts through Delta; the
route/return arrows r, s as the Kummer-pairing-shaped maps between
the chi and chi*-dual legs — as REAL maps where the campaign's
machinery supplies them, as NAMED INTERFACES where the arithmetic
representation rho is genuinely missing (that is the withheld summit;
do not fake it, localize it). The closed word C = sr as an actual
endomorphism of the chi-leg wherever constructible. The integral
Stickelberger guard and the #-transformation guard from
LinkingInterfaces bind here.

## W3 — obstruction, receipts, outcome

- The obstruction m: the statewise Fermat classes (the allocated pair
  from StateFactorPair) lifted into the carrier through ClassCarrier
  receipts.
- Receipt 1: the reflection-cycle relation on m (from the fold/7d
  machinery — re-expressed as a corner element acting on m).
- Receipt 2: the Stickelberger CONVERSION on m (Theta m lands in the
  units leg, recorded by beta — conversion, never premature
  annihilation), and/or the auxiliary-prime interface (the lamp) as
  the second corner element.
- The typed outcome, attempted honestly: either the unit-ideal
  identity e = u*c + v*a in the corner acting on m (whose projection
  is the gauge reading — record what it reads), or the named common
  factor = the surviving livelock channel, or the localized wall
  where rho is missing. All three are results. The endpoint and
  transformer remain forbidden regardless of outcome.

## House rules (binding)

PREDICTIONS first (which leg of the carrier resists; where rho bites
first; what W0's derivation will say — commit the guess before
computing). FINDINGS at discovery. Generic over p, 59 only in
instances; all guards and the 8,697-job verification stay green; new
guards per public theorem; standard trio. No endpoint, no transformer,
no dg. ps before heavy builds; truthful commits; the wall's exact
address is a deliverable.

— Fable (reviewer), on behalf of Fabian, 2026-08-05
