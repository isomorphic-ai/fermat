# TASK TRANSFER-COMPLETE — finish the map: only the three seams may remain non-literal

**TruthSeed:** `fermat-ledger:every-row-literal-or-a-named-seam`
**For:** PRO Goblin (GPT 5.6 Sol Ultra) via codex, working in ~/fermat.
**Mandate (Fabian, 2026-08-02):** continue. The TRANSFER session flipped
nine rows and met the tunnel; this session completes the sweep. Success
criterion: after this session, every row of notes/BOUNDARY-MAP.md is LITERAL
except exactly the three named seams (Lemma I / 7a, the transformer
successor, layer transport) — which remain untouched, guarded, and
stated in Transfer vocabulary. Still NO seam crossing, NO endpoint.

## The remaining rows, in order

1. **N6/N7 state enrichment** (the named blocker from last session):
   enrich the successor types to CARRY the originating factor state
   (the oriented successor and the septic branch states currently
   discard it), then build their Ledger adapters and accounted
   transfers like N3/N4/N5. The old theorems stay derivable as
   projections; nothing downstream may break (holdsAt_six/seven
   endpoints keep compiling, guards stay green).
2. **N1 adapter**: the vacuum rung's Ledger ℕ adapter (stock =
   charge u + charge v, credit = coupling, converted = 0) with the
   endpoint theorems routed through conservation_identity.
3. **The 59 instance accounting**: the conductor-59 specialization of
   accountFlow — identify the table's depth/coupling/surplus with the
   credit and converted amounts consumed by repayment; flip the
   Instance rows (deepExponentForcing_*, BernoulliCertificate rows,
   DepthCertificate mapping to one funded layer).
4. **Legacy C3** (`repay_of_deep_generated_cycle`): route the verdict
   theorem through the graded d=1 transfer so it stops being a
   parallel vocabulary (derive, do not duplicate).
5. Any remaining DECORATIVE/ABSENT row in the map not covered above,
   sweep in leverage order. If a row genuinely cannot flip without
   crossing a seam, say so in the map with the Transfer that would be
   required — that row becomes part of the seam's boundary statement.

## W-final — the map, closed

Update notes/BOUNDARY-MAP.md in place (before→after preserved). End state:
a closing section "The remaining frontier" listing exactly the three
seams (plus any row proven seam-blocked above), each with its required
Transfer written out. Verification gains a guard per new LITERAL row.
The map's summary line should be one sentence a stranger can check:
"every classified row is LITERAL except the following named seams."

## House rules (binding, unchanged)

PREDICTIONS entry first (which enrichment resists; whether N6/N7
endpoints survive the state change untouched). FINDINGS at discovery.
Generic core over p; literals in instance layers; all existing guards
and grep gates stay green; the tunnel (IsoConserveBridge) must keep
compiling. No Lemma I, no transformer inhabitant, no layer-transport
inhabitant, no endpoint. ps before heavy builds; truthful commits;
a finished row beats two half rows.

— Fable (reviewer), on behalf of Fabian, 2026-08-02
