# TASK FOLD — rung C6 statewise: net the two views, principalize the draw

**TruthSeed:** `fermat-n59:debit-and-receivable-are-one-matrix`
**For:** PRO Goblin (GPT 5.6 Sol Ultra) via codex, working in ~/fermat.
**Target:** discharge `Fermat.Conservation.KummerDrain.FactorPrincipalizationPermit`
for the Fermat-produced state — the exact typed hole in
TransformerProbe.lean — closing W2 of notes/TRANSFORMER-TASK.md.

## The construction (Fabian's mirror + the flt9 splice reading)

The W2 obstruction (Q^p principal but Q not known principal) is the
irregularity's minus side, and the repair is STATEWISE, not global:
only the special Fermat-produced classes must die, never the whole
class-group component. Two ledger relations kill them. For the state's
relevant quotient classes I, J (p-torsion by the stock rungs):

  (7a)  [I] + (p-1)[J] = 0   — the Lemma-I side: derived from the
        state's unit/repayment structure (our flow world).
  (7d)  [I] + [J] = 0        — the FOLD side: the real relative norm
        of the state's generator nets the two views of the ledger.
        Debit and receivable are one matrix; conjugation is the
        transpose; the norm is the fold that cancels them.

Then p[J] = 0 (p odd) with [I] = [J] and [I] = -[J] forces
[J] = [I] = 0: the draw is principal. Group theory is trivial; the
substance is deriving 7a and 7d from the state's actual equations in
ledger form — honest norm bookkeeping on the factor ledger, statewise.

## The work

W1 — `Fermat/Experiments/Conservation/Credit/Fold.lean` (generic): conjugation as
the ledger transpose on the class/debit ledger (the Vacuum rung's
`opposite` machinery lifted to classes); the relative-norm fold
operation; the generic netting lemma (odd-torsion class equal to both
another class and its negative is zero). Generic over p; no literals.

W2 — the statewise derivations: from the Fermat-produced state
(KummerDrain's AllocatedFactorLedger / the historical-state shape),
derive 7a via the repayment/flow side and 7d via the real relative
norm of the state generator. Credit Vandiver (Lemma I, equations
7a-7d) by name as discoverer; reconstruct in ledger form (the N3
pattern) — the classical `Fermat.Irregular.*`, `Fermat.KummerIso.*`,
`Fermat.Regular.*` namespaces remain FORBIDDEN imports with executable
guards.

W3 — discharge `FactorPrincipalizationPermit` for the state and fill
the TransformerProbe hole. If it closes with budget left, proceed to
TRANSFORMER-TASK W3 (successor state + charge-measure bridge:
lambda-drain charge vs z.natAbs via norm multiplicativity) and then W4
(Sophie-Germain Case I, assembly, holdsAt_fiftyNine_conservation,
crown audit). Endpoint only if every seam actually closes.

## House rules (unchanged, binding)

PREDICTIONS entry first (where 7d resists; whether the fold lemma
stays generic). FINDINGS at discovery. Generic core over p, 59 only in
the instance layer, grep gate extended. Verification.lean guards for
every public theorem; standard trio; forbidden names proven unknown.
Standalone green cones; ps before heavy builds; truthful commits;
typed obstruction probes are honorable deliverables.

— Fable (reviewer), on behalf of Fabian, 2026-07-30
