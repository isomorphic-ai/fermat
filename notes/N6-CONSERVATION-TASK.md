# TASK N6-CONSERVATION — FLT(6) purely via conservation, NO fold through 3

**TruthSeed:** `fermat-n6:native-or-forced-fold`
**For:** PRO Goblin (ultra) via codex, in ~/fermat.
**The research question (Fabian):** can a composite exponent be drained
NATIVELY by conservation, or is the divisibility fold the ledger's own
composition law appearing as necessity? Both outcomes are results.

## The structures available natively at 6

- ℚ(ζ₆) = ℚ(ζ₃): the SAME Eisenstein ring, new ledger decomposition —
  N(x + yζ₆) = x² + xy + y², and the degree-6 ledger identity
  a⁶ + b⁶ = (a² + b²) · N(a² − b²ζ₆).
- The balance-at-the-top route: a⁶+b⁶=c⁶ ⟹ (a³)² + (b³)² = (c³)² — a
  Pythagorean triple OF CUBES; the n=2 balance engine parametrizes,
  the cube constraints propagate, a charged descent must close below.
- The shared conservation floor (Fermat/Experiments/Conservation/Floor) as always.

## The a/b/c ladder (honest statuses; any rung is a deliverable)

(a) **Native theorem:** `Fermat.Six.holdsAt_six_conservation :
    Fermat.HoldsAt 6` with a cone containing NO exponent-3 Fermat
    statement — the drain runs directly at 6 (you may rediscover
    3-shaped descent structure INLINE; you may not import the theorem).
(b) **Conditional:** the native drain closes modulo named, separately
    falsifiable lemmas (each stated exactly).
(c) **Obstruction map — the forced-fold theorem:** if the native drain
    structurally reduces to needing FLT(3)-as-a-statement, TYPE that:
    isolate the exact point where every native route manufactures the
    exponent-3 equation, prove the reduction is forced on a typed
    object, and state it as a theorem about composite-exponent
    conservation ("the ledger drains composites only through prime
    folds"). This would be the first structural result ABOUT the
    conservation framework itself — a first-class outcome.

## Import discipline — the strictest cone yet

FORBIDDEN (and covered by forbidden-name-unknown guards):
- fermatLastTheoremThree and Mathlib's FLT modules;
- this repo's Fermat.Three.* — BOTH the classical Dirichlet/Euler route
  AND Fermat.Three.Conservation (your own N3 theorem);
- `Fermat.HoldsAt.mono_of_dvd` and every Ladder fold/transport lemma —
  the shortcut itself is forbidden, not just its inputs;
- anything proven FROM the above (check provenance).
FREE in bounded roles (listed): Eisenstein/quadratic ring machinery,
Mathlib PythagoreanTriple, parity/coprimality/squares, the shared floor,
your N2/N4/N5 conservation modules (balance engine, floor — these are
conservation-native, not folds).

## Gates
PREDICTIONS first — and pre-register WHICH rung you expect (a, b, or c)
and where the pain will be; FINDINGS at discovery; commit early/often;
executable Verification.lean REQUIRED (axiom guards + forbidden-name
guards incl. mono_of_dvd); N6-cone build green; standard trio; no
sorry/admit/axiom; whitespace clean.

Deliverable: either the first native composite-exponent conservation
proof, or the first theorem about WHY there isn't one. The framework is
under test, not just the exponent.
— Fable (reviewer), on behalf of Fabian, 2026-07-28
