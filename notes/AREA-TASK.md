# TASK AREA — carry D=2 through the tunnel; reread the seams under the new light

**TruthSeed:** `fermat-area:the-ledger-was-the-shadow`
**For:** PRO Goblin (GPT 5.6 Sol Ultra) via codex, working in ~/fermat.
**Provenance:** IsoConserve Heisenberg payload landed at
~/scheduler/iso-conserve-lean commits e5b23b5/f7b8e6f/ea920d9 (41
theorems, trio-clean, independently audited). This session transports
D=2 into the fermat tree. NO seam crossing, NO endpoint; the seams get
REREAD, not attempted.

## W1 — vendor the carrier (the IsoConserveStatements discipline)

Vendor the minimal Heis carrier + payload statements from
iso-conserve-lean (provenance header: source path, commit, SHA-256 per
file — toolchains differ, do not fight them; port the small proofs to
this tree's Lean where cheap, restate-and-reprove where trivial).
Deliver in the route-neutral core: `Fermat/Experiments/Conservation/Heis.lean`
with the group laws, center, abelianization hom, commutator = area.

## W2 — the abelianization theorem and AreaTransfer

1. Prove: the existing `Fermat.Conservation.Ledger` columns are the
   D=1 abelianization of the payload carrier — the compiled statement
   that the entire current campaign is the endpoint shadow of D=2.
   Wire it into IsoConserveBridge so the tunnel now carries the
   payload dictionary too (both directions where cheap; one direction
   with the other as named interface if the vendored statements make
   the converse heavy).
2. `AreaTransfer`: extend Transfer with the payload word — before and
   after carry (columns, payload); composition uses the twisted law;
   the existing Transfer is the abelian projection (derive, never
   parallel). Projection lemmas: every AreaTransfer projects to a
   Transfer; chains project to chains.
3. Port the two jewels as fermat-side theorems on AreaTransfer:
   no-erasure (c -> 0 not representable by any admissible composition)
   and exact-cancellation-iff (the pairing law).

## W3 — reread the three seams in area vocabulary (READ ONLY)

Update notes/BOUNDARY-MAP.md's frontier section: for each seam state what it
looks like at D=2, as a conjecture-shaped requirement, clearly marked
PREDICTION not theorem:

- Lemma I / 7a: state the on-record prediction — 7d was the endpoint
  half of the class relations and 7a is the AREA half (the reflection
  as holonomy bookkeeping; conjugation = orientation reversal = area
  sign flip; the two views netting endpoint-wise while the area
  coordinate carries the residual). Write the candidate AreaTransfer
  whose existence would BE 7a. Do not prove it.
- The transformer successor: which payload word the descent step must
  carry so the successor's receipt equals its enclosed area.
- Layer transport: the depth-D filtration reading — graded C_d as
  commutator depth; what the D=3 carrier would add (named only; Magnus
  tower explicitly out of scope).

## House rules (binding)

PREDICTIONS entry first (where the port resists; whether the
abelianization theorem is rfl-grade like the tunnel round-trip or
needs work). FINDINGS at discovery. All existing guards, grep gates,
and the 8,691-job verification stay green; new guards for every new
LITERAL claim; Heis.lean enters the grep gate (no prime literals in
the carrier). No Lemma I, no transformer inhabitant, no layer
transport, no endpoint. ps before heavy builds; truthful commits; an
honest port note beats a heroic reproof.

— Fable (delegated), for Fabian, 2026-08-03
