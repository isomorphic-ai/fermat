# The Conservation Audit — FLT(59) as a Ledger That Never Loses a Cent

**Snapshot:** `~/fermat` on goblin through `1542a34` (the literal conservation receipt for the canonical 827 ledger, following the no-lift normalized endpoint `367b613`, 2026-08-18). Tracked content only; untracked probe/scratch files from parallel exploration are excluded. The conjugation-fixed divisor `P * conjugate(P)` now yields a literal finite-`S` source whose canonical reflected `(59,44)` projection is both class-silent and nonzero. Lean constructs the genuine 827 lift, proves its class-silent pointed coordinate nonzero at every place over 827, constructs pointed incidence, proves the reflected boundary functional nonzero, and inhabits the whole normalized reflected fiber. The normalized coefficient and conditional endpoint now install that lift internally, while retaining the exact `processes ↔ gauge = 0` circularity. The globally `zeta`-normalized ledger likewise has no lift parameter; it is nonzero while its scalar total is zero, and is now routed through `PlaceLedger`, a zero-spent vacuum `Transfer`, and the scheduler-L1 identity. A genuine global Hilbert-reciprocity producer, comparison with all 58 local orbit readings, the required outside-place silence, and an independent comparison with the class-valued (7a) gauge remain absent.

---

## 1. ELI5 — what this campaign actually is

**A proof does not destroy the unknown. It processes it.**

Most people picture a mathematical proof as a demolition: a big unknown stands there, the proof knocks it down, and at the end there is nothing left. This campaign is built on the opposite picture — the picture of a **bank ledger**. At every moment there is a fixed total of "open question." Each step of the proof takes some of that total and moves it from the *still-open* column to the *certified* column, and — this is the discipline — **the remainder is never deleted, rounded away, or forgotten**. It is retained, named, and carried forward. Every step must balance:

    total  =  processed (certified, with a receipt)  +  still-open (conserved, with a name)

The Lean structure at the bottom of the whole repo says exactly this (`Fermat/Experiments/Conservation/Ledger.lean`):

    structure Ledger where
      stock credit converted total : α
      conservation : stock + credit + converted = total

A proof step is a `Transfer`: an accounted transaction with a `spent` field, an `available_eq` law, and a `total_preserved` law — you may move value between columns, you may never lose it. Later the campaign discovered that this ledger is literally the dimension formula of cohomology (**dim B + dim H + dim B_out = dim C** — "credit *is* cohomology"), and finally that it is the exact-sequence law of *observation itself*: for any readout `f`, retain `0 → ker f → V → im f → 0`. The campaign sentence: **conserve the fiber; process the quotient.** What your instrument reads is processed; what your instrument cannot see (`ker f`, the fiber) survives, on the books, unerased. The proof ends only when every entry of the input inventory has been processed — and an executable audit (`Verification.lean`, thousands of kernel-checked jobs, `#print axioms` frozen to the standard trio, `#guard_depends_on` on every public theorem) re-checks the balance at every station.

**Two more house metaphors you will meet below.** A **meter** is a covector — a thing that *reads* values (a voltmeter). A **probe** is a class — an actual element you can *insert* into a pairing (a test charge). They have opposite variance, and the single most important STOP in the campaign (Ulam W0) fired because the architecture had produced a beautiful meter where the task needed a probe; relabeling one as the other is the named failure mode, and the campaign refused. And a **receipt** is mechanical evidence — a named theorem, a green job count. In the audit table below, the *Processed* column contains only things with receipts.

**Why 59? The gauge idea.** Fermat's Last Theorem for exponent 59 is *already proven in this same repository* by the classical Vandiver–Takagi–Furtwängler route (`Fermat.holdsAt_fiftyNine` — kernel-checked, done). The conservation campaign locks that entire proof behind forbidden-import guards and asks a different question: *can the same territory be re-crossed so that every step is a conserving transaction?* So 59 is not the prize. **59 is the measuring instrument** — the first irregular prime that is small enough to compute with and irregular enough to be honest (a regular prime's credit ledger is the trivial vacuum; 59 actually has to borrow). The house rule makes this literal: every law is proved **generically over p** in the route-neutral core (`Fermat/Experiments/Conservation/`), and 59 and its attestation prime 827 appear only in the instance layer (`Fermat/Exponents/FiftyNine/Conservation/`). The deliverables are the invariants themselves — Ledger, Transfer, AreaTransfer, the cohomology ledger, the readout ledger — calibrated against 59 the way a new thermometer is calibrated against a known boiling point. The one theorem still open on this route, Vandiver's Lemma I ("relation 7a"), is the exact place where the instrument currently reads "not yet" — and the campaign's honesty rule is that it must *say* "not yet," in types, rather than fake a reading.

---

## 2. THE AUDIT TABLE

One row per station, chronological. **Processed** lists only mechanically evidenced items (theorem/def names live at HEAD; job counts from FINDINGS.md). **Still open** is what the station explicitly retained. Axes: 1 substrate, 2 structure, 3 focus, 4 geometry, 5 alignment, 6 agency, 7 potential.

| # | Step | Input (total potential) | Processed (receipt) | Still open (conserved) | Axis used | Audit |
|---|------|------------------------|---------------------|------------------------|-----------|-------|
| 1 | **Clean-cone charter + N59 spine** (07-29) | All of FLT(59), with the classical proof walled off: which parts survive as conserving transactions? | `Ledger.conservation_identity`, vacuum, `Ledger.repay`, shared descent `Floor`; N59 gauge facts (unit rank 28, `charge(λⁿ)=59ⁿ` strict drain) — `Conservation/Ledger.lean`, `Floor.lean`, `FiftyNine/Conservation/Spine.lean` | The entire arithmetic content: capacity, repayment, both Vandiver relations, successor, endpoint | 5 alignment (+1 substrate) | `Verification.lean` green 8,520/8,520; forbidden-prefix guards prove every classical N59/irregular/ladder name **unknown**; axiom trio frozen |
| 2 | **Stock spine N1–N7** (07-29→08-02) | Do the seven small-exponent proofs (n=1..7) fit the ledger vocabulary? | All seven rungs re-routed LITERAL through Ledger/Transfer (e.g. `Fermat.Two.charge_ledger`, N4 `charged_descent_transfer`, N7 `gauge_decomposition`); heterogeneous `stockSpineReceipt` | No common carrier across the seven fields (receipt stays heterogeneous); no N59 successor from any spine | 1 substrate | BOUNDARY-MAP "Stock receipt N1–N7" table, every row final-LITERAL via `#guard_depends_on`; N3–N7 verification leaves |
| 3 | **Credit C1–C2: vacuum + capacity** (07-29) | How big is 59's borrowing account (the credit the irregular prime draws)? | C1 accounted vacuum (`kummer_credit_accounted_vacuum`); **`capacityCertificate`** (`CapacityCertificate.lean:972`): generated 28×28 residue matrix at 827 nonsingular ⇒ real-unit capacity finite and prime to 59; `boundedSinnottBridge` (`BoundedSinnott.lean:341`) ⇒ **59 ∤ h⁺** | Whether the credit can be *repaid* (C3); everything class-group-side (minus part) | 7 potential | Kernel-checked matrix inverse, no `native_decide`; `827 = 2·7·59+1`, root `671 = 2^14` generated not postulated; axiom trio exact |
| 4 | **Credit C3 + Flow: repayment** (07-30) | Do deep draws on the account get paid back? | **`deepFlowLaw59`** (`Instance.lean:569`) and `repayment_of_capacity_and_flow`: every deep real unit is an actual 59th power; Bernoulli depth-2 row 21 accounted (`DepthCertificate.depthTwoCertificate`) | Repayment lives in the **real-unit carrier** — no transport to ideal classes; higher layers d>1 (LayerTransport seam) | 3 focus | Flow cone grep gate; `repay_layer_transfer` spends exactly 1; funded-row mapping conditional and explicit |
| 5 | **Gauge quotient** (07-30) | Does the generated real-unit action change the audited charge? | `GaugeQuotient.primeData` (three permits); `debitLedger_quotient_eq_bot`, quotient charge = N7 norm, invariant under the 28-unit gauge | W2's first new carrier (fractional-ideal quotient): gauge invariance says nothing about class-group principalization | 4 geometry | Faithful `sourceToQuotientTransfer` retains the full matrix debit; guarded in Credit Verification |
| 6 | **Fold: state allocation + (7d)** (07-30) | From an actual hypothetical Fermat solution, which class relations follow? | `stateLinkedIdealPair_exists` / `allocatedPair` (allocation from the real state, conjugation *derived*, not assumed); **`StateLinkedIdealPair.vandiverSevenD`** (`StateFactorConjugation.lean:231`) — relation (7d) proven statewise via relative norm through the real subfield | **Relation (7a)** = Vandiver Lemma I = `VandiverSevenA 0 1` = principality of `plusIdeal · minusIdeal^58` — named as *the* seam; permit consumers compile conditional on it | 2 structure | `f1c2591` "Guard the Vandiver Lemma I boundary"; counterexample recorded: in ZMod 59, (torsion + 7d) ⇏ (7a), so the gap is genuine |
| 7 | **Ledger-literal + Transfer retrofit** (08-01→08-02) | Are the proofs *actually consuming* the ledger, or is it decoration? | Route-neutral `Transfer` (available_eq, converted_decomposition, total_preserved); `IsoConserveBridge` L1 tunnel with proven round trips; **every classified row LITERAL or a named seam** (BOUNDARY-MAP published) | The three named seams: (7a) producer, stock-credit transformer, higher layer transport | 6 agency (+5) | Mechanical rule: LITERAL only if `#guard_depends_on` reaches the anchor **through the proof value**; DECORATIVE/ABSENT states retired row by row |
| 8 | **Area / Heisenberg D=2** (08-03) | Transfers with equal endpoints can differ by route — where is that difference kept? | `Heis` vendored (commit+SHA provenance); `AreaTransfer` = Transfer + ordered word + central area payload; no-erasure law = injectivity of `c ↦ center c · word`; exact cancellation iff `word.c = −c` | Whether any *arithmetic* word realizes (7a) as central cancellation (prediction only, flagged as such) | 4 geometry | TransferVerification extended to Heis/payload anchors; abelianProjection proven = old Transfer (D=1 is the shadow of D=2) |
| 9 | **Seam-7a crossing** (08-04) | Can the D=2 machinery *produce* (7a) as an area-cancellation transaction? | **Typed obstruction**: `vandiverSevenA_of_areaTransfer_to_vacuum` proves the only available direction (a wanted-endpoint AreaTransfer already *contains* (7a)); `TransformerProbe` guards the reverse; missing producer identified as Vandiver Lemma I / Leopoldt 1958 Spiegelungssatz | (7a) itself — the crossing **failed honestly**: `total_preserved` demands `r₀ + 58·r₁ = 0` *before* the word field can act; no map from `word.c = −c₇a : Prop in ℤ` into the class group | 6 agency | Green 8,693/8,693; the no-input probe stops at exactly `pair.ledger.VandiverSevenA 0 1`; nothing imported from the forbidden cone |
| 10 | **Interaction + Linking algebra** (08-04→08-05) | What does (7a) look like inside the two-account interaction algebra? | Strict twisted route algebra on the involutive base; **earned** swap quotient; `sevenARelationWord_eigenspace` / `sevenDRelationWord_eigenspace`: **(7a) is the −1/difference mode, (7d) the +1/common mode** — discharging **ERRATA E1** (labels had been reversed); period-2 livelock g=2 recorded (reflection is never a descent) | Which third-party structure (lamp/Stickelberger) reads the difference mode — neither required nor excluded by the algebra | 2 structure | E1 logged (08-05), fix *derived by computation* in `A_swap`, not asserted; linking verification leaf green |
| 11 | **Common-action stage** (08-05) | Put both accounts (Selmer χ, dual of Selmer χ*) on one stage with one action | Paired common-action carrier bound; real units routed into Selmer; corrected relation eigenspaces on the stage | Character-allocation seam: both Kummer character allocations, omega-dual laws, integral guards, beta compatibilities; `strictRouteRhoWall` behind them | 2 structure | `stage: audit the common-action boundary` (bc7c96b line of commits); seam addresses named in BOUNDARY-MAP frontier §1 |
| 12 | **Vendor-Selmer + PowerRoot cube** (08-05→08-06) | Is the campaign's "gauge defect" a generic naturality defect or genuine arithmetic? | Provenance-pinned generic `PowerRoot.obstruction` (fork PRs, SHA-256 recorded); `toClass_ker`/`toClass_range` derived generically; unsplit principal-ideal extension with **mechanical no-product-splitting audit**; `gaugeNaturalityOutcome59` = **OUTCOME 4**: the wild route is necessary, the defect is not generic | The wild (ramified, at-59) reading itself; `bocksteinPowerRootReceiptObservation` retains the `59·r₁` correction as a named observation | 7 potential | Byte-identical vendoring with SHA receipts; LinkingVerification freezes axiom trio; former Selmer-exactness seam **closed** by the two vendored theorems |
| 13 | **Tate bridge: conditional master** (08-06) | Route (7a) through local/global duality: every local draw belongs to one global sum | Place-indexed pairing + adjoint law (INTERFACE); `PlaceLedger.toLedger` / `reciprocity_L1_conservation` **proven** (reciprocity's zero-sum carried literally through Ledger/Transfer/L1); `N59BankReceipts` proven (capacity, Sinnott, flow, repayment, (7d) as receipts); **conditional master**: interfaces ⇒ exactly `VandiverSevenA 0 1` | `GlobalReciprocityLaw` (no producer); detector existence; gauge/pairing comparison; **named risk `mu_59_to_the_n`** (mod-59 may be too shallow; `58 = −1` only at the first layer) | 5 alignment | Green 8,706/8,706; one shared detector witness indexes all three targets (no witness-switching); risk executable: `58 ≠ −1` in ZMod 59² |
| 14 | **Tame symbols, stage 1** (08-06→08-07) | The tame half of every local reading, made explicit | `TameSymbol.Context`: degree-p Hilbert symbol in ZMod p with bilinearity, antisymmetry, Steinberg, both-units silence, Kummer descent; both Selmer legs seated; place rows realized and discharged | The **wild** place over 59 (explicitly outside the tame symbol); angular-component/Delta realization seams | 1 substrate | `tame: record the Stage-1 boundary` (409ff04); LocalOrthogonalityGuard retained where it binds |
| 15 | **Cohomology ledger** (08-07) | Is "credit" a metaphor or a theorem? | **`finrank_cell_ledger`** (`CohomologyLedger.lean:215`): dim B + dim H + dim B_out = dim C **instantiated into `Ledger`** (stock+credit+converted=total); exact filtration retained (dimension = lossy shadow); `Lambda` defined; `Lambda_apply_eq_zero_of_reciprocity` = **arithmetic Stokes** (tame faces cancel; wild face zero net flux) | New single frontier named: `wild_detector_faithful : ker Λ = ⊥` (later demoted to historical-sufficient); no map from SelmerChi to the class-valued gauge | 7 potential (+5) | No-splitting guard extended to the filtration cell; third major IsoConserveBridge instance; axis-3 correction recorded (docs only) |
| 16 | **Artin–Hasse fork** (08-07) | Can banked special values cover every input the wild pairing must read? | Coverage audit compiled: `campaign_formulaBudget_eq_needsVostokov` (`ArtinHasseInventory.lean:233`) — verdict **NEEDS-VOSTOKOV**, the two actual inputs (statewise Selmer lift, transverse detector component) are opaque to the advertised generator subgroup | The wild formula budget: explicit 59-local decompositions of both actual inputs | 5 alignment | Committed prediction `AH-SUFFICES` **falsified and recorded**; closed inventory verdict uses **no axioms at all**; 8,717/8,717 |
| 17 | **Detector witness: empty-support no-go** (08-07) | Does the 827 lamp give a live transverse detector in the seated carrier? | Lamp arithmetic live: first capacity coordinate `48 : ZMod 59`, edge residue `105`, `105¹⁴ = 803 = 671⁴⁸`; **no-go theorem** `EmptySupportTransverseDetectorWitness.not_nonempty`: the seated empty-support dual forces every 827 reading to zero | A carrier whose local condition is *relaxed* at 827; fallback primes identified as a livelock channel (structurally same no-go) | 1 substrate | Positive prediction falsified and recorded; three failing routes documented with distinct reasons |
| 18 | **Q-relaxed carrier repair** (08-07) | Rebuild the reflected dual so 827 is allowed to speak | `SelmerCarrierAt` (Mathlib arbitrary-support Selmer, literal); generalized `characterProjectorAt` idempotent + eigenlaw; `computedQReading_ne_zero` conditional on the named lift; exact list of where empty support had been load-bearing | `ReflectedQRelaxedLocalizationLift827` (global class with nonzero projected q-coordinate); `SelectedTameComparison` | 1 substrate | 8,717/8,717 at `6288ba6`; kernel bookkeeping proven: `supportValuation_ker_eq_range_emptySupportInclusion` |
| 19 | **Finite-S lift attempt** (08-08) | Lift the selected 827 coordinate to an honest global class | Vendored finite-S Selmer sequence (SHA-pinned); `exists_qRelaxedSource_of_sClass_torsion` (range leg **proven**); `projectedCandidate…_pow_eq_one` (torsion proven); `ofSource_of_sClassObstruction_eq_one` (representative kernel leg **proven**) | **No lift inhabitant**: joint hypotheses `hcoord` (nonzero projected coordinate) and `hobs` (obstruction = 1, not just torsion) unproved; `rhoQ` still an arbitrary supplied representation | 7 potential | Committed positive prediction **falsified and recorded**; 8,717/8,717 incl. both no-splitting audits |
| 20 | **Focus / SteeringFiber** (08-08) | Given class + silence constraints, what readings are even reachable? | `SteeringFiber.reachableReadings`: attainable set = affine coset `λx₀ + λ(ker ρ ∩ ker T)`; fixed ⇔ image ⊥ ⇔ joint-dual factorization; `GaugeSteering827` instantiated **post-projector** (scale 48 preserved); both future branch receipts compiled | The one missing computation: `λ(ker ρ ∩ ker T)` = ⊥ or ⊤ — **branch undecidable with present machinery** (a coverage verdict, not a math verdict) | 3 focus | 8,719/8,719; explicit no-product-equivalence audit on the new namespace; W3 not entered per gate |
| 21 | **Transversality W1–W4: FIXED at 827** (08-09) | Which way does the conserved one-dimensional local rank point? | W1 `SplitPrimeFourier827`: 58 places = one regular Galois orbit; delta⇄character Fourier duality proven. W2 `FocusConormal`/`ExteriorTransfer`: the old Boolean is the zero/nonzero shadow of a retained conormal class / Plücker coordinate. W3 `PointedTateIncidence827`: first exact leg proven; `primalGain + reflectedGain = 1`. W4: **conditional verdict FIXED, allocation (0,1)** — class-dual pullback activates | At this checkpoint `QLocalizationEquivariance827`, both Poitou–Tate exactness fields, and a producer for `rhoQ` were separate. Row 28 constructs the incidence fields from seating plus a nonzero q-relaxed lift; row 31 proves the canonical seating | 4 geometry (+3) | 8,724/8,724; W1's "Fourier alone decides" prediction met its pre-stated falsifier and the stop was taken; no parallel Boolean introduced |
| 22 | **Ulam W0: type freeze — STOP** (08-13) | Does the verdict architecture hand W1 an actual reflected object, or only numbers? | Detector budget theorem: `finrank (H_FLT / ker gauge_7a) ≤ 1` (rank–nullity — Ulam's only lawful finite-field shadow); `reflectedBoundaryFunctional827` exposed as a genuine localization **covector**; class-valued gauge question typed (`ClassValuedSevenAGaugeSeating`, zero ⇔ `VandiverSevenA 0 1`); both kernel directions separated | **STOP fires**: the covector is a **meter, not a probe** — wrong variance for the needed class `y*`; W0 red, Lane 1 closed; `ker G` retained, never erased | 3 focus | 8,642/8,642 + N59 8,649/8,649; committed STOP guess confirmed *by variance check*, not by name-matching; refusal list enforced (no functional fabricated from a count) |
| 23 | **READOUT / Ulam W1: observation conservation** (08-13) | Unify the three W0 holes under one law: conserve the fiber, process the quotient | `ReadoutLedger.lean`: `conservedReadout_exactSequence` (0→ker f→V→im f→0 retained); `processedRangeEquiv` (equal kernels ⇒ canonical `im f ≃ im g` — **the invariant**; unit proportionality only its coordinate shadow); affine-fiber + rank-one factorization lemmas; `NormalizedReflectedFiber827` retained as *public carrier*, **conditionally nonempty** from incidence+Fourier; depth audit `59 • im G = 0` confirmed at the correct carrier | At this checkpoint the typed list still included separate wild lawfulness, `ker Λ ≤ ker G`, reciprocity, and routing surviving `ker G`; the fiber was never collapsed to a privileged y*. Row 27 later derives lawfulness from reciprocity and consumes a fiber member internally | 5 alignment | 8,643/8,643 + 8,651/8,651; every W1 statement carries its exact sequence; existing conormal/steering laws **consumed via bridges**, not re-derived |
| 24 | **VOSTOKOV V1–V3** (08-13, **narrowed; canonical branch now inhabited**) | The wild 59-local reading itself — or its exact residual core | V1: `campaignResidualInventory_eq` — residual is exactly **two** kinds `[statewiseSelmerLift, transverseDetectorComponent]` (one predicted; prediction corrected). V2: `WildKummerPairing.Core.ofRepresentative` — generic bilinear descent through both Kummer quotients **proven**. V3: `ReflectedWildKummerCoreAt59` (3 fields) + constructor `toReflectedWildLocalizationAt59` (`VostokovLocalization59.lean:396`): core ⇒ complete localization, **hole strictly shrunk by theorem** | Row 25 records the historical tier-(c) coordinate stop. Row 26 inhabits all three core fields and the localization for the interface canonically restricted from the normalized continuous pairing; row 37 later constructs the genuine reflected lift. Equality with an independently supplied historical reading and every post-lift arithmetic discharge remain separate | 1 substrate (+7) | Original 8,654/8,654 constructor cone retained. The later inhabitants do not retroactively validate the original prediction that a supplied historical reading would be calibrated |
| 25 | **VOSTOKOV C1 + tier-(c) reduction** (08-17, **historical typed stop**) | Do the literal pairing shapes fit a cheap formula, and can that formula fill the three-field core? | `VostokovShapeAudit59`: exact classes/reps/readback; both whole classes retained as residuals; normalized 827 fiber proved outside the strict range. Tiers (a) and (b) refuted for the tracked target. `TotalAugmentedCoordinates.representative`: total trace-product pairing defined and descended. `descend_eq_oldReading`: independent representative comparison implies old quotient calibration; `toReflectedWildLocalizationAt59`: reduction + action compatibility fires localization | This coordinate-chart branch still has no producer for its total-coordinate realization or independent Hilbert/wild comparison. Tier (c) is **not refuted**, so forbidden tier (d) remains untouched. Row 26 bypasses those coordinates for the canonical self-restricted interface, but does not supply their historical comparison | 3 focus (+5 alignment) | Three committed predictions remain scored honestly (C1 and tier-(b) falsified; session inhabitance at that checkpoint not met); no fabricated values, split, `axiom`, or `sorry` |
| 26 | **Critical norm detector + normalized continuous wild localization** (08-18, **inhabited canonical branch**) | Can the exact critical norm-image gap normalize the genuine continuous cup and fill the old-shaped localization without inventing a historical comparison? | `ExactNormIntersection59` proves `range(Norm) ⊓ U_59 = U_60` and reduces critical norm membership and literal field-norm existence to `criticalCoefficient59 = 0`. `normalizedInflationReadout59` selects a normalized algebraic readout; its genuine continuous cup yields total local/global pairings. `oldWildInterfaceOfPairing` restricts the pairing and proves the full `hash omega` adjoint law. `normalizedReflectedWildKummerCore59` and `normalizedReflectedWildLocalization59` inhabit the complete canonical core and q-relaxed localization | The selected scalar map is not claimed continuous or canonical as a local invariant. Equality with a separately supplied historical interface is exactly the universal calibration seam. Rows 27–29 internalize boundary nonvanishing and a fiber member, derive lawfulness from reciprocity, construct incidence, and expose the lift-facing endpoint. Row 30 locates its circularity; row 31 constructs canonical seating; row 32 retains the generic auxiliary-column alternative | 2 structure (+5 alignment) | Standalone three-target cone green, 8,798/8,798; four representative endpoints audit to exactly `[propext, Classical.choice, Quot.sound]`. The total pairing is defined from the continuous cup, not from the old reading; self-calibration is definitional and no Case II, (7a), reciprocity, fiber, or FLT endpoint is inferred |
| 27 | **Normalized Ulam endpoint + reciprocity-derived lawfulness** (08-18, **conditional endpoint tightened**) | Which previously separate Ulam premises can the canonical one-column localization discharge by composition? | `normalizedBoundaryFunctional59_ne_zero` and `normalizedReflectedFiber59_nonempty` obtain the boundary receipt and retained-fiber inhabitance from `PointedTateIncidence827` plus `QLocalizationEquivariance827`. `wildLawfulness827_of_reciprocity` proves the full kernel law from the existing one-column reciprocity interface. `vandiverSevenA_of_normalizedContinuousReciprocity` internally supplies localization, boundary nonvanishing, a fiber member, and lawfulness | The theorem's literal signature takes incidence and Fourier seating, `ClassValuedSevenAGaugeSeating`, `WildProcessesAtLeastSevenA`, and `NormalizedTameSilenceReciprocity59`. Row 28 removes incidence as an independent producer by constructing it from a nonzero lift plus seating. Reciprocity has no producer; lawfulness is discharged only conditionally from it | 5 alignment (+6 agency) | `NormalizedContinuousReadout59` target green, 8,797/8,797. No unconditional (7a), reciprocity, kernel/fiber collapse, Case II, or FLT result is inferred |
| 28 | **Algebraic pointed incidence** (08-18, **independent incidence seam removed**) | Are the two stored Poitou–Tate exactness fields genuinely new inputs once attention is fixed and reflected localization is nonzero? | `pointedTateIncidence827_of_fixed_of_localization_ne_zero` constructs both exactness fields algebraically. `pointedTateIncidence827_of_fourierSeating_of_lift` specializes this: Fourier seating gives fixed attention and `ReflectedQRelaxedLocalizationLift827` gives the nonzero reflected localization, hence together they construct `PointedTateIncidence827` | At this checkpoint both lift and seating were open. Row 31 later constructs canonical seating. The constructor selects no complement or splitting; gauge seating/processing and reciprocity remain unchanged | 2 structure (+5 alignment) | `AlgebraicPointedIncidence827` target green, 8,554/8,554. Incidence is conditionally discharged, not postulated or claimed unconditional |
| 29 | **Direct lift-facing normalized endpoint** (08-18, **intermediate conditional signature**) | Can callers use the reduced transversality inputs without manually assembling incidence? | `normalizedPointedIncidence59OfLift` composes the algebraic incidence constructor at the canonical q-relaxed representation. `normalizedWildCoefficientOfLift59` derives both incidence and lawfulness. `vandiverSevenA_of_normalizedContinuousLift` directly compiles the full chain to relation (7a) | At this checkpoint a nonzero lift and its seating remained explicit, together with gauge seating, processing, and reciprocity. Row 30 exposes the circularity; row 31 removes the seating premise but not the lift | 6 agency (+5 alignment) | `NormalizedContinuousReadout59` green, 8,798/8,798. The theorem is formally valid as a conditional/regression endpoint, but is not a non-circular proof of (7a), Case II, or FLT |
| 30 | **Zero-coefficient circularity audit** (08-18, **exact proof boundary**) | Does the reduced lift-facing signature actually process the class gauge, or does one remaining premise already assert the target? | `wildCoefficient827_eq_zero_of_reciprocity`, `normalizedWildCoefficientOfReciprocity59_eq_zero`, and `normalizedWildCoefficientOfLift59_eq_zero` prove that reciprocity annihilates the one-column coefficient. `wildProcessesAtLeastSevenA_zero_iff` and its normalized/lift specializations prove `processes ↔ gauge = 0`. `vandiverSevenA_of_classGauge_eq_zero` obtains (7a) directly by evaluating that zero gauge at the Fermat class | The present `WildProcessesAtLeastSevenA` premise is not an independent bridge: under reciprocity it is stronger than and directly contains (7a). A finer nonzero readout or genuinely independent gauge comparison is required; lift, gauge seating, and reciprocity producers remain absent | 3 focus (+5 alignment) | `29b3f29` + `d3c9ead`; normalized target green 8,799/8,799. Circularity is exposed by exact iff theorems rather than prose; no false endpoint claim, kernel erasure, or fiber collapse |
| 31 | **Canonical cyclotomic Fourier seating** (08-18, **seating seam closed**) | Does the actual cyclotomic action/localization satisfy the contragredient 827-place law needed by Fourier? | `cyclotomicQLocalizationEquivariance827` identifies the regular orbit with the canonical cyclotomic place action and combines valuation covariance with the reflected eigenspace law. `normalizedQLocalizationEquivariance827` installs it in the normalized branch. `vandiverSevenA_of_normalizedContinuousCanonicalLift` no longer accepts seating; `normalizedCanonicalLiftProcessesAtLeastSevenA_iff_gauge_eq_zero` preserves the exact circularity diagnostic | The nonzero `ReflectedQRelaxedLocalizationLift827` remains uninhabited. Gauge seating and reciprocity remain open, and the processing premise still already contains (7a) | 5 alignment (+2 structure) | `1b90e07` + `73daf9a`; no complement, splitting, or fabricated lift. The stronger API reduction does not weaken the zero-coefficient warning |
| 32 | **Auxiliary-column reciprocity retention** (08-18, **generic escape shape compiled**) | Can reciprocity preserve information instead of killing the only retained column? | `pairAt_add_pairAt_eq_zero_of_outside_two` proves `wild + auxiliary = 0` when all outside rows vanish; `pairAt_eq_neg_pairAt_of_outside_two` orients it as `wild = -auxiliary`. The auxiliary receipt is retained rather than erased | No arithmetic auxiliary place/pairing or gauge comparison is constructed. Row 33 proves that a canonical lift is a full 58-place wave, so this is a structural prototype rather than the literal p=59 support shape | 7 potential (+5 alignment) | `1f5c0de`; generic finite-support/reciprocity algebra only. No new arithmetic premise is hidden or declared inhabited |
| 33 | **Exact reflected-lift criterion** (08-18, **existence boundary sharpened**) | What arithmetic statement is actually equivalent to inhabiting the remaining q-relaxed lift? | `nonempty_reflectedQRelaxedLocalizationLift827_iff` identifies generic lift existence with nonvanishing of some `classSilentPointedCoordinate827` on the projected-class kernel. `nonempty_cyclotomicReflectedQRelaxedLocalizationLift827_iff` makes it an iff at any chosen 827-place. Every lift forces the projected finite-S obstruction to one, and `qLocalizationCoordinate_ne_zero_at_every_place_of_lift` plus `tameSilence827_ne_zero_of_lift` prove full-wave support | The named class-silent coordinate nonvanishing is unproved. No lift, class-group computation, or splitting is produced. Full-wave support requires all 58 orbit coordinates to be retained; row 35 later performs that sum and Fourier compression conditionally on the lift | 3 focus (+4 geometry) | `a88c356`; dedicated target green 8,557/8,557. Both directions of lift existence compile; the former sufficient constructor is now an exact criterion, not an inhabitant |
| 34 | **Tame bookkeeping to two-place balance** (08-18, **generic wrapper compiled**) | Can explicit tame vanishing feed the retained auxiliary-column reciprocity law without restating every row? | `OutsideTwoTameBookkeeping.distinguished_eq_neg_auxiliary_of_reciprocity` and the seated `Realization` wrapper combine proved tame rows with one residual non-tame audit, then conclude `distinguished = -auxiliary` from global reciprocity | At this checkpoint the residual audit and arithmetic auxiliary comparison remained inputs, and canonical p=59 full-wave support prevented literal one-auxiliary support. Row 35 supplies the required whole-orbit/Fourier successor instead | 5 alignment (+6 agency) | `8148225`; `LinkingVerification` green 8,544/8,544 and guards reach both tame bookkeeping and generic two-place reciprocity. No p=59 arithmetic instance is claimed |
| 35 | **Fixed-root 827 tame detector + Fourier nonvanishing** (08-18, **p=59 full-wave detector compiled**) | Can the p=59 route retain all 58 genuine local symbols in the historical fixed-root coordinate, orient them correctly, and prove that detector ledger is live? | `LocalReduction827` constructs each 827 tame context and identifies its fixed-attestation-root coordinate with the inverse-oriented raw residue times the genuine localization coordinate. `CyclotomicTameContext59` proves silence at every tame place outside 59/827. `PrimalFourierNonvanishing827` identifies a computable `58 × 28` matrix with the 28 generated units and kernel-checks all 28 nontrivial even modes. The resulting **unweighted fixed-root sum** is nonzero | Lift existence and certification of the chosen mode remain explicit inputs. A supplied unweighted equation implies a nonzero wild scalar algebraically, but that equation is not literal global Hilbert reciprocity. Row 36 supplies the global-root correction; no abstract `pairAt`, (7a), or FLT endpoint is promoted to an actual value | 5 alignment (+3 focus, +6 agency) | `3acb637..8064f23`; focused endpoint green 8,567/8,567. The declarations use exactly `[propext, Classical.choice, Quot.sound]`; proof-value and no-product-equivalence audits pass |
| 36 | **Global-root normalization and canonical cancellation** (08-18, **coordinate correction compiled**) | What changes when every local finite logarithm is oriented by the reduction of one fixed global cyclotomic root, as global reciprocity requires? | `TameSymbolTransport` proves residue-presentation invariance and primitive-root rescaling. `CanonicalGlobalTameLedger827` proves `actualFixedRoot(tau) = tau⁻¹·canonicalGlobal(tau)`, so the canonical total is `∑ tau, tau·actualFixedRoot(tau)`. `CanonicalGlobalTameLedgerIrregular827` kernel-checks the selected odd mode and proves the canonical ledger is nonzero but its total is zero | A genuine global Hilbert-reciprocity producer is still absent. If supplied for this canonical ledger, its equation forces the wild scalar to zero. The old unweighted nonzero detector cannot be used as the global reciprocity total, so the independent class-gauge comparison must target a different nondegenerate invariant or the live place-indexed ledger rather than that scalar total | 2 structure (+5 alignment, +3 focus) | `e0d4ff6..29d69af`; named builds green. Audited endpoints use exactly `[propext, Classical.choice, Quot.sound]`; no new axiom, provider, or reciprocity producer |
| 37 | **Canonical conjugate-pair lift and concrete global ledger** (08-18, **lift seam closed**) | Can one actual 827-supported class satisfy class silence and reflected-mode nonvanishing simultaneously, then remove the lift parameter from every geometric/global consumer? | From `P * conjugate(P)`, `ConjugatePairSource827` retains plus-class principal provenance and a literal 827-unit source. `CanonicalConjugatePairProjection827` computes its `(59,44)` coordinate as `reduction(1/58)·(-2) ≠ 0`; `ConjugatePairClassSilence827` proves its projected finite-`S` class obstruction is one. `canonicalConjugatePairLift827` constructs the genuine lift, and `canonical_classSilentPointedCoordinate827_ne_zero` proves nonvanishing at every 827 place. `CanonicalConjugatePairIncidence827` constructs pointed incidence, boundary nonvanishing, and a nonempty retained fiber. `CanonicalConjugatePairGlobalLedger827` and `CanonicalConjugatePairNormalizedEndpoint59` install the lift without an argument; the ledger is nonzero with total zero, while the normalized branch preserves `coefficient = 0` and `processes ↔ gauge = 0` | The global-reciprocity producer, comparison of the pairing with all 58 orbit values, required outside-place silence, and an independent non-circular class-gauge comparison remain inputs. Relation (7a), Case II, and FLT do not follow | 6 agency (+5 alignment, +4 geometry) | `afc4942`, `a334561`, `8be7524`, `ca4048f`, `fd9b5b7`, `3966a65`, `367b613`; incidence 8,822/8,822, normalized endpoint 8,823/8,823, and concrete ledger 8,640/8,640 green. Public declarations audit to `[propext, Classical.choice, Quot.sound]`; no split, new axiom, provider, product equivalence, or per-prime lift certificate |
| 38 | **Canonical 827 conservation receipt** (08-18, **literal ledger/transfer/L1 routing**) | Does the proved nonzero zero-total tame ledger enter the repository's generic conservation tunnel without being relabeled as reciprocity? | `canonicalConjugatePairPlaceLedger827` retains the concrete `Finsupp` and its zero-total proof as `PlaceLedger`; `canonicalConjugatePairVacuumTransfer827` is its zero-spent transfer to `Ledger.vacuum`; `canonicalConjugatePairVacuumTransfer827_L1` exports the exact scheduler-L1 identity. `canonicalConjugatePairPlaceLedger827_entries_ne_zero` proves the cancellation is live | This receipt proves only the explicit tame-ledger balance. It constructs neither a place-indexed Kummer--Tate pairing nor global Hilbert reciprocity, all-orbit comparison, outside-place silence, the class-gauge bridge, (7a), Case II, or FLT | 5 alignment (+6 agency) | `1542a34`; 8,641/8,641 green, exact standard axiom trio. No reciprocity producer is hidden in the conservation packaging |

---

## 3. Walkthrough — one plain paragraph per row

**1. The charter.** Before anything, the campaign locked the vault: the finished classical proof of FLT(59) lives in this same repository, and an executable guard proves its every theorem name is *unknown* inside the new workspace. Then it minted the bank: a ledger with three columns (stock, credit, converted) and one law — the columns always sum to the total. Nothing may ever be proved by peeking at the old proof; everything must be re-earned as balanced transactions.

**2. The training wheels.** The proofs for exponents 1 through 7 were re-plumbed so that each one literally *runs through* the ledger — not "is consistent with" but "consumes in its proof value," checked by a dependency guard. This established the audit discipline on territory where the answers were already known: seven receipts, each on its own route, honestly recorded as *not yet* one common ledger.

**3. Opening the account.** An irregular prime is a prime that needs credit: its arithmetic "wants to borrow" against a class group. Station 3 measured the credit line. Using a bigger helper prime (827, which is *derived*, not chosen — it is the minimal certified step above 59 in the generation chain) as a lamp, a 28×28 table of readings was computed and kernel-checked to be invertible. Meaning: the account is finite, and its size shares no factor with 59. Bonus theorem: 59 does not divide the "plus" class number.

**4. Repayments clear.** Whenever the arithmetic takes a *deep* draw on the account (a unit congruent to a 59th power to very high depth), that draw is genuinely repaid — it *is* a 59th power. This is a real theorem (`deepFlowLaw59`), proved with analytic weighting machinery (logarithmic derivatives — axis 3, "a little of this, a little of that"). Crucially: the repayment happens in the *real-unit* currency. The ledger notes, honestly, that this currency is not the class-group currency where the big debt lives.

**5. Rotating the meter doesn't change the balance.** There are 28 "gauge" directions in which you can re-coordinate the books without touching physical content. Station 5 proved the audited charge is blind to all of them. Like checking your bank balance is the same in euros displayed left-to-right or right-to-left — necessary hygiene, and it tells you the remaining problem is real, not notational.

**6. The fork in the debt.** Now assume the enemy exists: an actual solution to x⁵⁹+y⁵⁹=z⁵⁹. From it the campaign *constructs* a pair of ideal accounts (I, J) — conjugates of each other, and that conjugacy is derived from the state, not assumed. Two relations would kill the enemy. One, called (7d), was **proven**: it routes I through a third structure (the real subfield) and back — a genuine three-party settlement. The other, (7a) — "I·J⁵⁸ is principal," Vandiver's Lemma I — resisted, and a counterexample shows it does *not* follow from what's banked. (7a) becomes the campaign's named open debt.

**7. The audit of the audit.** Station 7 went back through every proof and asked: does the conservation law appear *in the proof itself*, or is it a sticker on the box? Each row was upgraded until a machine check confirmed the proof value physically travels through the ledger — or the row was stamped SEAM with a name. Result: everything is literal except three named seams. No silent gaps.

**8. Routes enclose area.** Two ways of moving the same money between the same accounts can still differ — by the *route*. Station 8 added a second story to the ledger (Heisenberg payloads): every transaction now also carries the signed area its route enclosed, and a no-erasure law guarantees route memory cannot be silently cancelled. The old flat ledger is exactly the shadow of this richer one.

**9. The honest failure.** Could (7a) be produced as an area-cancellation trick — two mirror routes whose areas cancel? Station 9 tried the crossing and the compiler said no, *precisely*: the flat part of the transaction already demands (7a) before the area layer even gets to speak. The needed producer is a genuinely different kind of theorem (a reflection principle from 1958 number theory). The failure was typed, guarded, and banked — the campaign's proudest habit is that its dead ends are theorems too.

**10. The mirror mix-up.** Working inside the two-account algebra, the campaign *earned* a quotient in which "common mode" (accounts moving together) and "difference mode" (accounts moving oppositely) become well-defined — and discovered its own earlier labels were backwards (Erratum E1). The proven relation (7d) kills the common mode; the open (7a) is the *difference* mode. The correction was derived by computation, and the erratum file preserves the mistake and its repair — errors are conserved too.

**11. One stage, one action.** Before any 59-specific computation may count as mathematics rather than numerology, both accounts must stand on the same stage under the same action algebra. Station 11 built the stage; the price tags still missing (character allocations and their compatibility laws) were posted as named interfaces.

**12. Credit is cohomology, wholesale.** A generic "obstruction to taking p-th roots" engine was imported with cryptographic provenance receipts, and the campaign's class-group machinery was re-derived from it. Key verdict (OUTCOME 4): the troublesome gauge discrepancy is *not* a bookkeeping artifact that generic nonsense could dissolve — it is genuine arithmetic living at the wild (ramified) place. The wild route is necessary. Also here: a standing audit that mechanically rejects any attempt to split an extension that doesn't canonically split — no fake change-making.

**13. Every local draw belongs to one global sum.** Class field theory's reciprocity law says: sum a certain reading over all places (all "branch offices") and you get zero. Station 13 wired that law *as a ledger*: the place-indexed readings are the stock, and the zero-sum is a transfer to vacuum. It then compiled the conditional master theorem: *if* someone supplies reciprocity, detector existence, and the gauge comparison, the output is exactly (7a). And it posted a warning flag (`mu_59_to_the_n`): the current instruments read only the shallowest layer; a deeper-living class would need a finer meter.

**14. The tame half, in full.** At every "tame" branch office (places where 59 is not ramified), the local reading was built explicitly — the Hilbert symbol with all its laws — and proven silent where both inputs are units. This clears the ledger everywhere except the single wild office over 59. Everything now funnels to one desk.

**15. The metaphor becomes a theorem.** The dimension formula of cohomology — boundaries + surviving classes + outgoing boundaries = everything — was instantiated *directly into the Ledger structure*. Credit is the middle term: retained potential, H as unspent balance. And reciprocity was re-read as Stokes' theorem: interior faces cancel pairwise; the boundary (the wild face) carries zero net flux. The frontier was renamed accordingly: not "find another conservation law" but "prove the one remaining detector actually sees the remaining potential."

**16. The coverage audit.** Before building heavy machinery for the wild reading, the campaign checked whether already-banked special values cover all the inputs it must read. Verdict, compiled: no — two inputs (the state's chosen Selmer lift, and the detector's component) are opaque to the banked generators. The honest budget line reads NEEDS-VOSTOKOV. The optimistic prediction was falsified and the falsification recorded.

**17. The lamp works; the socket doesn't fit.** The helper prime 827 really does supply a live, computable, nonzero reading (the number 48, checked in the kernel). But the carrier the campaign had seated its dual classes in was too strict — its fine print forces every 827 reading to zero. A no-go theorem was proven *about the real carrier*. Trying other helper primes was recognized as a livelock (same fine print, same zero) and refused.

**18. Relax exactly one clause.** The repair: replace the carrier by one whose local condition is relaxed exactly at the places over 827 — and document precisely which old proofs had been leaning on the strict clause. The no-go dissolves; the next wall (a global lift with the right nonzero coordinate) is named, not assumed.

**19. Two legs close; the knot doesn't.** Freshly vendored exact-sequence machinery proved both bookkeeping legs of the lift (every torsion class comes from somewhere; kernel elements have unit representatives). But the *joint* condition — a source that is simultaneously nonzero in the right coordinate and trivial in the right obstruction — was not obtained. Prediction falsified, recorded, conserved.

**20. What can attention reach?** Route-neutral theorem: given constraints (right class, silence elsewhere), the set of achievable pointed readings is an exact affine line — either a single point (FIXED: the reading is determined) or everything (STEERABLE: a genuinely new degree of freedom). At 827 the deciding computation — the image of one intersection of kernels — is not yet in the tree. Verdict: undecidable *with present machinery*, and stated exactly that way.

**21. Fourier says FIXED — after one IOU.** The 58 branch offices above 827 form one perfectly regular orbit, and on such an orbit position-space and frequency-space are exact duals: a spike at one office contains every frequency; a pure frequency touches every office. So if the arithmetic action is properly seated on this orbit (the one named missing law), the pointed reading cannot be independent: verdict FIXED, and the conserved one-dimensional rank lands entirely on the reflected-dual side — allocation (0,1), the class-dual pullback future. Both hypotheses remain explicit IOUs; unconditionally, the station is an honest typed stop.

**22. Meters are not probes — STOP.** The Ulam audit asked the architecture: you promise a reflected bit; hand me the *object* that carries it. What existed was a covector — a meter that reads classes — while the task needs an actual class — a probe to insert into the pairing. Opposite variance; no relabeling allowed. The STOP fired exactly as pre-registered. Also banked: the question (7a) needs only a ≤1-dimensional quotient of the potential (a detector *budget* theorem — you need one bit to answer a one-bit question), while `ker G`, everything the gauge cannot see, stays on the books untouched.

**23. The observation conservation law.** The unifying law of the whole campaign, now generic: an observation processes its quotient and conserves its fiber — keep the exact sequence 0 → ker f → V → im f → 0, never split it, never delete ker f. Two instruments with the same blind spot are *canonically* the same instrument on what they process (the range equivalence is the invariant; "g = u·f" is only its coordinate shadow). The 827 instance re-seated everything in this vocabulary: the whole normalized fiber — not a chosen favorite point — is the public carrier, nonempty *conditionally* on the two transversality IOUs.

**24. The wild desk itself (narrowed, then inhabited on one canonical branch).** V1 computed the exact residual inventory — two items, not the predicted one; V2 proved that any honest bilinear pairing on representatives descends to Kummer classes; V3 shrank the hole *by theorem* to a three-field core which constructs the entire localization structure. Row 25 preserved the historical coordinate-formula stop. Row 26 later filled that same core type for the old-shaped interface obtained by restricting a newly constructed normalized continuous pairing.

**25. The two labels become terms, and tier (c) becomes code.** C1 proved that the actual inputs are arbitrary old-primal and q-relaxed Kummer classes with classically chosen quotient representatives—not the two normalized factors suggested by the provenance labels. Both remain complete residuals for the historical Artin–Hasse coordinate chart, and a normalized 827 fiber point is provably not an old strict point. Thus tiers (a) and (b) do not cover that declared total chart. At tier (c), Lean constructs a total representative pairing as a trace product of two additive coordinates, proves bilinearity and descent, derives landing from ambient action compatibility, and derives old calibration from an independent comparison on the audited representatives. Its norm-coherent coordinates and independent historical comparison remain absent; tier (d) was not touched. The later continuous-cup route does not pretend to fill those terms—it reaches the canonical interface by a different, now-inhabited branch.

**26. The critical norm gap normalizes a real pairing.** The completed norm calculation proves exactly which critical units are norms and turns its nonzero cup into a normalized algebraic readout on genuine continuous `H²`. Composing that readout with the real cup product gives a total pairing, and restricting that pairing to the old seats automatically supplies the adjoint law, the three-field core, and the full q-relaxed localization. The calibration receipt is definitional only because the old-shaped interface was constructed from that same pairing. Matching an independently supplied historical reading remains precisely the universal comparison theorem; the downstream (7a) machinery remains untouched.

**27. Reciprocity pays the lawfulness bill once.** The normalized readout adapter sends the canonical localization directly into the Ulam endpoint. Incidence plus Fourier seating prove the boundary functional nonzero and make the retained normalized fiber nonempty, so neither becomes a separate endpoint argument. Because the place-indexed pairing has one wild column, reciprocity forces its complete wild reading to vanish and therefore supplies lawfulness on the conserved 827 kernel. Reciprocity is still an IOU; the gain is that it is no longer accompanied by a duplicate lawfulness IOU. Row 28 then reduces the remaining incidence input to the nonzero-lift/Fourier-seating pair.

**28. The five-term package folds into two visible coordinates.** Fixed attention makes the primal conormal arrow zero; a nonzero reflected localization makes the connecting arrow injective and identifies its image with the required annihilator. Lean packages those two calculations as both fields of `PointedTateIncidence827`. Fourier seating supplies fixed attention, while a `ReflectedQRelaxedLocalizationLift827` supplies the nonzero value. So incidence itself is no longer a separate IOU. At this station the lift and seating were still IOUs; row 31 later pays the seating bill, and no complement or splitting is chosen.

**29. The endpoint speaks the reduced API directly.** This intermediate adapter accepts the still-open nonzero q-relaxed lift and Fourier seating, builds incidence, obtains boundary nonvanishing and a member of the retained fiber, derives lawfulness from the still-open reciprocity law, and fires the relation-(7a) consumer. This removes manual assembly from the caller without proving any remaining premise. It is a useful compiled regression boundary; row 30 audits its processing premise, and row 31 later removes seating from its public API.

**30. Lean catches the circular bridge.** Reciprocity does two jobs in the one-column model: it proves lawfulness, but it also kills the whole coefficient. A zero meter has every input in its kernel, so asking `ker Lambda ≤ ker G` now says that every input is also invisible to the gauge—that is, the whole gauge map is zero. Evaluating that statement at the Fermat input already proves (7a). Lean records every step as exact equalities and iff theorems. The endpoint is sound, but it has not crossed Lemma I; it has only repackaged a premise that already contains it. The honest next meter must be finer and nonzero, or the gauge comparison must be justified independently.

**31. The Fourier seat becomes canonical code.** The regular 827-place orbit and the actual cyclotomic place action turn out to have exactly the same orientation. Valuation covariance, applied with the inverse automorphism, then matches the inverse reflected character demanded by the Fourier theorem. Lean now builds the seating law rather than asking the caller for it. The strongest endpoint therefore needs only the reflected lift as geometric input. This API improvement is real, but it does not change row 30: the canonical-lift coefficient is still zero under one-column reciprocity, and its processing premise is still whole-gauge vanishing.

**32. Keep a second receipt instead of zeroing the first.** The one-column model says “everything except wild is silent,” so reciprocity has no choice but to set wild to zero. The generic two-place theorem keeps one auxiliary column on the ledger. Reciprocity then says exactly `wild + auxiliary = 0`, or `wild = -auxiliary`; information moves between two accounts instead of disappearing. This is a structural prototype, not yet the p=59 support pattern. Row 33 proves that the actual lift lights up all 58 places above 827.

**33. The missing lift becomes one exact nonvanishing.** A lift used to carry several representative fields and look like a bundle of independent mysteries. Lean now proves those fields contain no extra existence content: a lift exists exactly when the selected q-coordinate is nonzero after restricting to the kernel of the projected finite-S class map. Canonical Fourier seating makes the criterion the same at any 827-place. The nonvanishing itself remains open. A hypothetical lift is also a full character wave—every one of the 58 coordinates is nonzero—so it cannot be compressed to a point mass by wishful bookkeeping.

**34. Tame rows feed the auxiliary balance—but the orbit is larger.** The generic tame realization now packages all explicitly silent tame rows and one residual non-tame audit directly into `distinguished = -auxiliary`. This removes repetitive support plumbing and confirms that the two-place conservation law composes with real tame bookkeeping. For p=59, however, the full-wave theorem says one auxiliary place is not enough. This station therefore stopped before pretending otherwise; station 35 supplies the complete 827-orbit sum and its honest Fourier compression.

**35. All 58 fixed-root lamps stay on the detector ledger.** Lean constructs the tame-symbol meter at every place over 827 and proves that physical place number `tau` reads residue coordinate `tau⁻¹`; the inverse orientation is data, not a relabeling. Every tame place outside 59 and 827 is silent. A `58 × 28` kernel computation checks every generated circular unit against every nontrivial even mode, so the **unweighted fixed-root detector total** is genuinely nonzero for a reflected lift. A supplied equation `wild + detector total = 0` therefore makes its wild scalar nonzero, but this equation is only an algebraic consumer premise—not global Hilbert reciprocity.

**36. One global root changes the total.** The fixed attestation root is not the reduction of the same global root at every orbit place. Lean proves the exact correction: the global coordinate at `tau` is `tau` times the old fixed-root coordinate. The globally normalized total is therefore weighted. In the canonical `(59,44)` mode that weight shifts the reflected exponent from 14 to 15 and selects inverse-oriented primal exponent 43; the kernel checks that coefficient is zero. The canonical ledger itself remains nonzero, but its total vanishes. A future genuine reciprocity producer must use this canonical ledger and would force the matching wild scalar to zero. No reciprocity theorem, class-gauge comparison, relation (7a), or FLT endpoint is fabricated.

**37. One honest two-prime source pays the lift bill.** Start with an actual prime `P` over 827 and keep `P * conjugate(P)` rather than choosing an unexplained divisor root. The maximal-real class-number receipt turns this fixed divisor into a literal 827-unit source while retaining why the choice is class-silent. Its orbit has exactly two raw lamps, at `1` and `-1`; the real character projector reads `reduction(1/58)·(-2)`, which Lean proves nonzero. The same projector stays inside the finite-`S` unit range, so its class obstruction is exactly one. Those two receipts construct a genuine reflected lift at every 827 place. At the explicit base place Lean now constructs pointed incidence, proves the boundary functional nonzero, and inhabits the retained whole fiber. Both the normalized endpoint and global ledger install the same fixed-base lift internally. The former still diagnoses `processes ↔ whole gauge = 0`; the latter remains live place by place and cancels in total. What remains is no longer “find a lift,” but identify the actual local pairings with that ledger and prove their global reciprocity, then build a non-circular comparison with the class gauge.

**38. The live cancellation enters the conservation tunnel.** The concrete global ledger is not merely accompanied by a theorem saying its sum vanishes. Lean now stores that family and proof as the generic `PlaceLedger`, sends it to the literal vacuum through a transfer that spends zero, and carries the equality through the scheduler's L1 bridge. The nonzero-entry theorem stays beside the zero-total theorem, so cancellation is never confused with an empty ledger. This is a receipt for the tame balance already proved, not a proof that these entries are all local readings of one globally reciprocal Kummer--Tate pairing.

---

## 4. The open frontier — the unprocessed-input list

Everything below remains a **named, typed, uninhabited interface, comparison seam, retained risk, or exact obstruction to the present formulation**. Canonical localization, the genuine reflected 827 lift, boundary nonvanishing at every 827 place, a fiber member, and separate lawfulness are no longer endpoint arguments. Nothing downstream on the (7a) route is unconditional. The strongest normalized endpoint is compiled, but its current processing premise under reciprocity already contains relation (7a); it is a regression endpoint, not a non-circular proof.

The former transversality item is closed: `canonicalConjugatePairLift827`
constructs the genuine lift, and
`canonical_classSilentPointedCoordinate827_ne_zero` proves the exact
class-silent nonvanishing criterion at every 827-place.  The resulting lift
is the previously predicted full 58-place wave; no fiber is collapsed.

**A. Localization comparison** (canonical branch processed):
1. If the campaign requires a separately supplied historical wild interface rather than the canonical self-restricted one, prove its universal calibration—and hence its equality—with `oldWildInterfaceOfPairing`. The historical tier-(c) coordinate realization and `IsComparedOn` theorem remain an optional comparison chart, not prerequisites for the normalized endpoint.

**B. Downstream of localization** (the exact zero-readout boundary):
2. `ClassValuedSevenAGaugeSeating` — the genuine `ClassPTorsion`-valued gauge `G` on `H_FLT` with the allocated difference class as its Fermat value.
3. A **non-circular independent gauge comparison** for a nondegenerate global-root invariant. The old `WildProcessesAtLeastSevenA` (`ker Λ ≤ ker G`) remains circular on the one-column branch because item 4 forces that old `Λ = 0`; `normalizedCanonicalLiftProcessesAtLeastSevenA_iff_gauge_eq_zero` turns it into `G = 0`, which already implies (7a). The p=59 full-orbit construction retains all 58 actual 827 values and proves the fixed-root unweighted detector sum nonzero, but the global-root correction proves the canonical scalar total zero. A future comparison must therefore use a different nonzero scalar, more local/place-indexed information from the live canonical ledger, or another genuinely nondegenerate invariant. (`WildUsesNothingBeyondSevenA`, the reverse direction, matters only after such a comparison exists.)
4. An actual global Hilbert-reciprocity producer for the genuine canonical conjugate-pair ledger. `pairAt_lambdaPlace59_eq_zero_of_canonicalConjugatePair827` has no lift premise, but still requires `GlobalReciprocityLaw`, comparison with `canonicalConjugatePairTameOrbitValue827` at all 58 orbit places, and silence at every other nonwild place. The concrete ledger is nonzero with scalar total zero, so those hypotheses force the wild value to zero. The abstract pairing law does not construct any of the three arithmetic inputs, and this does not remove item 3's gauge-comparison problem.

**C. Beyond (7a)** (even with (7a) closed, FLT(59)-by-conservation still needs):
5. Common-action character allocation (frontier §1: both Kummer character allocations, omega-dual laws, integral guards, beta compatibilities, then `strictRouteRhoWall`).
6. The stock-credit transformer successor (seam §3: the strict-descent step from a Fermat state — with the D=2 prediction that its receipt equals its enclosed area).
7. Higher layer transport (seam §4: `LayerTransport` for d > 1).
8. **Named risk `mu_59_to_the_n`**: all current instruments read mod 59; if the (7a) class lives deeper in the 59ⁿ tower, the meters must be upgraded to `ZMod (59^n)` — the `59·r₁` correction is retained in `BocksteinPowerRootReceiptObservation` against exactly this contingency.

Historical sufficient detours explicitly demoted (kept, not deleted): global `ker Λ = ⊥` (`wild_detector_faithful`), the old empty-support `transverse_detector_exists` seating, and the scalar `gauge_eq_local_tate_pairing` comparison.

**The balance line, today:** total = FLT(59). Processed = the entire tame world, both bookkeeping halves of the credit account (capacity + repayment), one of the two class relations (7d), the full conservation superstructure (Ledger → Transfer → Area → Cohomology → Readout, all generic over p), the exact critical norm detector, the canonical normalized localization and Fourier seating, and the genuine canonical reflected 827 lift. The lift receipt includes a literal plus-provenance finite-`S` source, identity projected class obstruction, and class-silent pointed nonvanishing at every 827 place; it now unconditionally yields pointed incidence, boundary-functional nonvanishing, and a nonempty whole normalized fiber. Both the normalized endpoint and the concrete global ledger install the lift internally. The normalized endpoint truthfully retains `coefficient = 0` and `processes ↔ gauge = 0`; the global ledger retains all 58 globally oriented values, is nonzero, and has scalar total zero. That exact tame cancellation is now routed through `PlaceLedger`, a zero-spent `Transfer`, and scheduler L1 without being called reciprocity. Still open (conserved and named) = items 1–8 above: optional historical-interface calibration, class-gauge seating, a nondegenerate independent gauge comparison, a genuine global reciprocity producer plus all-orbit pairing comparison and required outside-place silence, and the beyond-(7a) obligations. No Case II, unconditional relation (7a), arithmetic reciprocity instance, kernel/fiber collapse, or FLT endpoint is smuggled across the boundary.
