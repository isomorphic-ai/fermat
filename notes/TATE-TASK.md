# TASK TATE-BRIDGE — the pairing gives numbers; reciprocity gives the law

**TruthSeed:** `fermat-tate:every-local-draw-belongs-to-one-global-sum`
**For:** PRO Goblin (GPT 5.6 Sol Ultra) via codex, working in ~/fermat.
**Provenance:** the analysis goblin via Fabian (emotional GD trace:
character evaluation +82, Selmer action +89, local Tate pairing +97,
global reciprocity +99). The bridge: Kummer-Artin/Tate pairing +
global reciprocity. Standing rules: NO unconditional 7a claim, NO
endpoint, NO transformer. The deliverable is the COMPILED CONDITIONAL
ROUTE with three named arithmetic hypotheses, each a typed, falsifiable
target — plus whatever of them the existing bank genuinely discharges.

## W1 — the pairing layer (interfaces with laws; realize the involution)

For the reflected pair carrier (the common-action stage), a
place-indexed family of local pairings into ZMod p with:

1. the ADJOINT LAW: pair_v (a • x) y = pair_v x (a# • y) — the
   arithmetic realization of the InvolutiveBase involution (the
   reflected characters chi * chi-star = omega are exactly the ones
   that pair; wire this to the existing # machinery so the linking
   algebra is realized by the pairing, not merely represented);
2. GLOBAL RECIPROCITY: finite support over places and
   sum_v pair_v x y = 0 for global classes — state it as the
   conservation identity on the place-indexed ledger (each local
   reading a column entry; reciprocity = L1; wire to
   Ledger/Transfer/IsoConserveBridge so the tunnel gains this
   instance — the prime-ledger conservation law, compiled).

Mathlib lacks local/global class field theory: these enter as NAMED
INTERFACES with their laws, same discipline as rho. Honesty about the
boundary is the point; the LAWS are the deliverable.

## W2 — the three named arithmetic theorems (typed targets)

1. `gauge_eq_local_tate_pairing`: there is a detector y and a unit u
   with pair_59 x y = u * (r0 + 58*r1) — the 59-local reading IS the
   computed gauge. (The gauge already exists formally; this names its
   pairing identity.)
2. `transverse_detector_exists`: the Poitou-Tate two-reading detector
   — a 59-only detector may be globally illegal; relaxing at an
   auxiliary prime q yields a global detector with exactly two
   readings (at 59 and at q). THE precise one-prime-serves-another
   theorem: q supplies the second local coordinate required for
   global legality. Wire the statement to the lamp interfaces from
   TransverseAnnihilator (the serving graph gets its arithmetic
   semantics).
3. `bank_silences_other_places`: every reading at v not in {59, q}
   vanishes — structured as a PLACE-BY-PLACE AUDIT consuming the
   existing bank where it genuinely applies (capacity, bounded
   Sinnott, flow/repayment, the fold/7d killing the q-reading), with
   the explicit ORTHOGONALITY GUARD: Selmer membership alone does not
   silence a place — the local condition on x must be orthogonal to
   the dual condition on y at each place; the audit table lists each
   place class, what silences it, and what remains an interface.

## W3 — the compiled conditional route

The master theorem: W1 laws + the three W2 hypotheses imply
u * (r0 + 58*r1) = 0, hence gauge = 0, hence (via the existing
vanishing-is-exactly-7a theorem) VandiverSevenA 0 1. One compiled
implication with every hypothesis named and typed. Mark per
hypothesis: PROVEN (which parts the bank discharges now) vs INTERFACE
(what awaits class field theory or the detector construction). Update
BOUNDARY-MAP: the 7a seam's entry becomes this three-hypothesis route.

## Guards

- The mu_{59^n} caution as a named note: the mod-59 layer first; if 7a
  lives deeper, the pairing must be 59^n-valued or the correction
  58 = -1 + 59 gets discarded instead of routed through the bank —
  record as a stated risk on the master theorem.
- No unconditional 7a, no endpoint, no transformer. PREDICTIONS first
  (which of the three theorems the bank gets furthest into; where the
  orthogonality audit bites). FINDINGS at discovery. All guards and
  the full verification stay green; new guards per public theorem;
  standard trio. ps first — a second session (genroot) runs in
  ~/Mathlib. Truthful commits.

— Fable (reviewer), on behalf of Fabian, 2026-08-06
