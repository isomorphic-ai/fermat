# TASK N7-CONSERVATION — prove FLT(7) strictly via the conservation law

**TruthSeed:** `fermat-n7:rank-two-gauge`
**For:** PRO Goblin (ultra) via codex, in ~/fermat.
**Predecessors:** N2 balance / N3 drain / N4 composition / N5 gauge
(rank-1) / N6 native-composite (state-coordinate fold). This rung's NEW
LESSON: **rank-2 gauge** — ℚ(ζ₇)'s unit lattice has rank 2 (two
fundamental units); the charge must be invariant under a
two-dimensional lattice of gauge transformations. The step from one
Pell unit to a unit LATTICE is the step toward the regulator — name it.

## The conservation structure (spine mandatory)

1. **The charge:** the ℤ[ζ₇]-norm (class number 1 — certify it or
   consume a bounded-role Mathlib/repo fact with provenance). Prove
   multiplicative conservation + `charge_gauge_invariant` for the FULL
   rank-2 unit lattice (the rung's named lesson).
2. **The ledger identity:** the septic composition — the campaign's own
   fold identity Ψ₇(X,Y) = (X−Y)⁶ + 7·X·Y·C² with C = X² − XY + Y²
   (from the historical folding notes) as the exact ledger
   decomposition; a⁷+b⁷ = (a+b)·Ψ₇-form as the ledger equation.
3. **The drain quantum:** λ = 1 − ζ₇, N(λ) = 7.
4. **The charged descent:** Lamé/Lebesgue-shaped case split, both/all
   branches draining the gauge-invariant charge into the SHARED floor.
5. **Assemble:** `Fermat.Seven.holdsAt_seven_conservation :
   Fermat.HoldsAt 7`.

## Import discipline (established; strict cone)

- FORBIDDEN (+ forbidden-name guards): this repo's classical
  Fermat.Seven.* (the Lebesgue 1840 route and anything proven from it);
  HoldsAt.mono_of_dvd and all Ladder folds; Mathlib FLT modules (none
  exist at 7 — the risk is in-repo again).
- FREE (bounded, listed): cyclotomic ring machinery, unit/lattice
  lemmas, quadratic subfield ℚ(√−7) facts, the shared floor, the
  N2–N6 conservation modules.
- Reconstruction-with-credit per the standing pattern (credit the
  repo's Lebesgue formalization in the header if adapted).
- Executable Verification.lean REQUIRED (axiom + forbidden guards).
- Cone independent of the flt-regular manifest drift.

## Gates
PREDICTIONS first (incl. where rank-2 gauge will bite); FINDINGS at
discovery; commit early/often; cone build green; standard trio;
no sorry/admit/axiom; whitespace clean. Honest fallback pre-authorized:
spine + named branch lemmas with exact statements.

Deliverable: the fifth rung — the conservation library acquiring the
unit-LATTICE vocabulary (rank ≥ 2) that the regulator, the circular
units, and Vandiver's lemma are written in. After this rung the library
speaks every dialect the generic splice's open seams require.
— Fable (reviewer), on behalf of Fabian, 2026-07-28
