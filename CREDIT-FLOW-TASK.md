# TASK CREDIT-FLOW — mint the flow reading; Lean as software engineering

**TruthSeed:** `fermat-credit:flow-is-the-derivative-of-the-generator`
**For:** PRO Goblin (GPT 5.6 Sol Ultra) via codex, working in ~/fermat.
**Supersedes** the seam-3 approach of CREDIT-SEAMS-TASK.md. Fabian's
ruling: importing or hoisting the classical Vandiver chain is the WRONG
move — needing a classical lemma means the architecture is missing a
primitive. The missing primitive is named below; Vandiver is credited
as its discoverer, not consumed as its source.

## The architectural insight (Fabian + Fable, 2026-07-30)

The credit ledger has three readings, like every level. We built its
stock reading (capacity = index, C2) and its conversion (repayment,
C3), but never its FLOW reading. The forbidden five-file chain is a
flow calculus that never names flow: logarithmic derivatives ARE the
rate reading of the multiplicative ledger. And the confirming jewel:

    t/(e^t − 1) = Σ B_n t^n / n!

The Bernoulli numbers are the Taylor coefficients of the generator's
log-derivative — not imported classical data, but the flow
coefficients of the credit generator itself. That is why they appear
in both the capacity certificates (stock) and the forcing lemma
(flow): one quantity, two readings — L4 at the credit level.

## Binding principle 1 — flow is DERIVED from the generator, always

`Credit.Flow` must be CONSTRUCTED from the generator object (the same
generator/orbit structure C2 already uses), never postulated as an
independent map. If a flow exists that cannot be re-derived from a
generator, that is a bug in the architecture — the level-3 principle
says generators are primary and flow is the derivative of their
recursion. Concretely: the dlog homomorphism is a function OF the
generator datum; its matrix, its eigenvalues, its L4 law all come out
of that construction. One source of truth.

## Binding principle 2 — Lean as software engineering, not math

We are heading for ANY prime, and eventually ALL. Therefore:

1. **The generic core is parameterized over an arbitrary odd prime p**
   (plus the structures below). Nothing in the core knows which prime
   it serves.
2. **Everything special about 59 lives in ONE thin instance file**
   (`Fermat/FiftyNine/Conservation/Instance.lean` or similar): the
   tower 7→29→59, the lamp 827, the generator 671, the irregular pair
   (59,44), the certified cube-free eigenvalue list. Each special fact
   enters through a NAMED structure field (a `CreditData p` /
   `FlowCertificate p` record), so instantiating prime 67 later means
   providing a record, not editing the core.
3. **Mechanical gate (the Kimi done-gate pattern):** outside the
   instance file(s), `grep -n "59" Fermat/Conservation/Credit/*.lean`
   returns nothing load-bearing (docstrings citing the campaign are
   fine; literals in statements/proofs are not — `(p := 59)` appears
   only in the instance layer). Cite the grep in the closing commit.
4. Interfaces are structures/classes with stated laws; theorems
   consume instances. The per-prime obligation should shrink to: one
   certificate record, generated by computation (the sweep
   infrastructure mass-produces these — that is the ANY→ALL road).

## The work

**W1 — `Fermat/Conservation/Credit/Flow.lean` (generic).** The Kummer
logarithmic-derivative homomorphism, derived from the generator datum:
units-mod-p-th-powers → coefficient space of rank matching the orbit.
Prove: (i) it is a homomorphism (flow of a product = sum of flows —
conservation-shaped by definition); (ii) the L4 law: λ-adic depth ≥
2p forces the flow coordinates to vanish (depth = integral of flow);
(iii) functoriality under the gauge action needed by W2.

**W2 — C5, the gauge diagonalization (generic).** In the character
basis the flow map is diagonal; the eigenvalue attached to character
k carries the Bernoulli coefficient (the elementary/Herbrand direction
only — do NOT attempt Ribet's converse). This is rung C5 of the
approved ladder, built properly instead of skipped. The basis-change
to the raw generated basis is the row-scaled Vandermonde your own
FINDINGS identified — state it generically (powers of a unit of the
orbit ring), instance-check it at 59.

**W3 — the forcing theorem (generic), then the 59 instance.**
Generic: deep congruence ⟹ (W1.ii) flow vanishes ⟹ (W2) character
coordinates killed wherever the eigenvalue is a p-unit-or-certified
⟹ (C2 capacity injectivity) the unit is a p-th power. The Bernoulli
cube-freeness enters ONLY as a certificate field (Fable's computed
list for 59 is in BERNOULLI-CUBE-CERTIFICATE.md and the existing
`NoBernoulliCubeObstruction59` shape — keep that interface).
Then: `DeepCoefficientForcing59` becomes an instantiation, closing
seam 3 with zero forbidden imports.

**Out of scope:** the stock/credit transformer (seam 4) and endpoint
assembly stay out UNLESS W3 closes cleanly with session budget left —
in that case the transformer may be attempted, and the endpoint only
if the transformer also closes, each with full guards.

## Credits and cone

Vandiver's Lemma II is the discovered assembly — credit him by name in
Flow.lean's header, alongside Kummer (the dlog map is his). The
forbidden list is UNCHANGED (all Fermat.Irregular.*, the five
Vandiver* files, classical 59 route, transport); the point of this
task is that none of it is needed. External pinned packages (Mathlib,
KummerCriterion) remain fair, boundary listed.

## House rules

PREDICTIONS entry first (which W resists, where the generic/instance
cut will fight back); FINDINGS at discovery; Verification.lean guards
extended (axiom trio + forbidden names + the new grep gate); ps before
builds; truthful commits; obstruction maps honorable. If W1's L4 law
needs a λ-adic valuation API that Mathlib lacks, building it generically
is in scope — that is exactly the kind of infrastructure this
architecture exists to mint.

— Fable (reviewer), on behalf of Fabian, 2026-07-30
