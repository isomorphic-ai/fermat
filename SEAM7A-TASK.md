# TASK SEAM-7A — the first crossing: close 7a by reflection

**TruthSeed:** `fermat-7a:the-route-and-its-mirror-cancel`
**For:** PRO Goblin (GPT 5.6 Sol Ultra) via codex, working in ~/fermat.
**Authorization (Fabian, 2026-08-04):** the mapping phase is complete;
this is the FIRST authorized seam crossing. Target: seam 1 only —
relation (7a) — by the Spiegelung route. "Let's see if it closes that
way." An honest obstruction map remains an acceptable deliverable; a
forced theorem is not.

## The wanted poster (from BOUNDARY-MAP.md, your own writing)

Construct the exact-cancellation transfer:

    τ₂ : AreaTransfer (Additive (ClassGroup (𝓞 K))) ℤ

from the canonical allocated state, WITHOUT accepting VandiverSevenA
as an input: abelianProjection with the displayed r₀ / 58 • r₁ /
vacuum / spent fields; beforePayload = Heis.center c₇a;
afterPayload = Heis.center 0; word = w₇a; payload equation =
exact central cancellation w₇a.c = -c₇a. Its projection IS
VandiverSevenA 0 1.

## The mathematical engine: Spiegelung (credit Leopoldt 1958 by name)

The prediction to realize: conjugation fixes the reflection word's
abelian endpoint and NEGATES its signed area (the fold machinery
already proves orientation reversal at the carrier level; the
exact-cancellation iff already says area -c is the unique zero
mechanism). The reflection word w₇a is to be BUILT from the state:
the statewise factor allocation (StateFactorPair /
StateFactorConjugation — 7d's machinery) composed with its conjugate
view. Classically this is Vandiver's Lemma I, whose deep mechanism is
the reflection theorem: the minus-side draw and its mirror image on
the plus side pair off; the plus side is certified silent
(boundedSinnottBridge, capacityCertificate). Statewise ONLY: the
special Fermat-produced classes, never the whole component. Sources
credited: Leopoldt (Spiegelungssatz), Vandiver (Lemma I), Kummer.
Forbidden namespaces unchanged (no Fermat.Irregular / KummerIso /
Regular imports); the toolkit is what this campaign built: Fold,
GaugeQuotient, Heis, AreaTransfer, the capacity and Sinnott theorems,
the flow/repayment machinery.

## Scope

- W1: predictions — where the construction resists: producing the
  incoming central receipt c₇a from the state? proving the conjugate
  word's area negation statewise? the ClassGroup carrier mechanics?
- W2: build w₇a and c₇a from the canonical allocated state; prove the
  conjugation/area-negation law for it; assemble τ₂; project to
  VandiverSevenA 0 1.
- W3: IF τ₂ lands: wire it into the existing conditional
  principalization (n59g's "conditional principalization from the
  remaining 7a relation") so FactorPrincipalizationPermit discharges —
  that wiring is a projection, not a new seam. Then STOP. The
  transformer successor and the endpoint remain out of scope even on
  success — one crossing per session, reviewable units stay small.
- If it does NOT land: the typed obstruction, localized to the exact
  sub-construction (which piece of the reflection is missing from
  Mathlib or from our carriers), in the map's PREDICTION section,
  same honesty as every prior refusal.

## House rules (binding)

PREDICTIONS first; FINDINGS at discovery; every existing guard, grep
gate, and the 8,693-job verification stays green; new guards for
every new public theorem; standard trio; statewise generic where
possible, 59 only in instance layers; ps before heavy builds;
truthful commits.

— Fable (reviewer), on behalf of Fabian, 2026-08-04
